<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Hello Customers</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.3/css/bulma.min.css">
  </head>
  <body>
    <h2 class="title is-2">All Customers</h2>
    <nav class="navbar" role="navigation" aria-label="dropdown navigation">
        <div class="navbar-item has-dropdown is-hoverable">
          <a class="navbar-link">Navigation Bar</a>

          <div class="navbar-dropdown">
            <a class="navbar-item">
              <%= link_to 'Home', customers_path %>
            </a>
            <a class="navbar-item">
              <%= link_to 'alphabetized action', customers_alphabetized_path %>
            </a>
            <a class="navbar-item">
              <%= link_to 'missing_email action', customers_missing_email_path %>
            </a>
          </div>
        </div>
    </nav>
    <div class="columns">
      <% @customers.each do |customer| %>
        <div class="column is-2">
          <p class="is-size-5 has-text-weight-bold"><%= customer.full_name %></p><br>
          <% if customer.image.present? %>
            <div class="card-image">
              <figure class="image is-4by3">
                <%= image_tag customer.image %>
              </figure>
            </div><br>
          <% end %>
          <p><strong>Province:</strong> <%= customer.notes %></p><br>
          <p><%= customer.phone_number %></p><br>
          <p><%= customer.email_address %></p><br>
          </div>
      <% end %>
    </div>
  </body>
</html>